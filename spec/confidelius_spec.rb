require 'spec_helper'

describe Confidelius do
  it 'has a version number' do
    expect(Confidelius::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
